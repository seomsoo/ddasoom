/** 기본 타입 설정 */
type UserId = number;
type Name = string;
type Email = string;
type PhoneNumber = string;
type DateString = string; // 날짜
type Token = string;

interface SearchParamsProps {
  searchParams: SearchParams;
}
